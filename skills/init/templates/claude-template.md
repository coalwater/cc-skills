<!-- cc-skills start -->

## Workflow defaults

- /caveman by default, ALWAYS
- /ground-truth by default — when a task would otherwise have you guess, infer, or assume (a bug to fix, an open question to answer, or a scoped change over a set you'd derive yourself), invoke the ground-truth skill first.

<!-- cc-skills end -->
